@inject BCKFreightTMS.Services.Data.IFinanceService financeService
@model IEnumerable<BCKFreightTMS.Web.ViewModels.Orders.ListAcceptedOrderViewModel>

<div class="p-2">
    <a asp-action="Accept" class="btn btn-primary">Create new order</a>
</div>
<hr class="mt-1" />
<div asp-validation-summary="ModelOnly" class="text-danger"></div>
<table class="table table-striped table-hover rounded" id="orders_data">
    <thead>
        <tr>
            <th>
                Order
            </th>
            <th>
                Client
            </th>
            <th>
                Price
            </th>
            <th style="visibility: hidden;"></th>
            <th style="visibility: hidden;"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in this.Model)
        {
            <tr>
                <td>
                    <span class="text-bold">@order.CreatedOn.ToLocalTime().ToShortDateString()</span>
                    <p class="m-1">@Html.DisplayFor(modelItem => order.Voyage)</p>
                </td>
                <td>
                    <span class="text-bold">@Html.DisplayFor(modelItem => order.OrderFromCompanyName)</span>
                    <p class="m-0">@($"{order.OrderFromContactFirstName} {order.OrderFromContactLastName}")</p>
                </td>
                <td class="align-middle">
                    <span class="text-bold">@order.OrderFromPriceNetIn </span>
                    <p class="m-0">@order.OrderFromCurrencyName</p>
                </td>
                <td class="align-middle text-center p-1" style="width:auto">
                    <a asp-action="Create" asp-route-id="@order.Id" class="btn btn-primary">Find carrier</a>
                </td>
                <td class="px-2 text-center align-middle" style="width:auto">
                    @*<a asp-action="Edit" asp-route-id="@order.Id" title="Edit"><i class="pr-2 fas fa-edit"></i></a>*@
                    @*<a href="javascript:void(0);" onclick="showInPopup('Order/Details/@order.Id', 'Details')" title="Details">
                    <span>
                        <i class="p-1 fas fa-info-circle"></i>
                    </span>
                </a>*@
                    <a asp-action="Delete" asp-route-id="@order.Id" title="Delete"><i class="fas fa-trash-alt"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts{
    <script src="~/plugins/datatables/jquery.dataTables.min.js"></script>
    <script src="~/plugins/datatables-bs4/js/dataTables.bootstrap4.min.js"></script>
    <script src="~/plugins/datatables-responsive/js/dataTables.responsive.min.js"></script>
    <script src="~/plugins/datatables-responsive/js/responsive.bootstrap4.min.js"></script>
    <script>
        $(function () {
            $('#orders_data').DataTable({
                "paging": true,
                "filter": true,
                "processing": true,
                "lengthChange": true,
                "searching": true,
                "ordering": true,
                "info": true,
                "autoWidth": false,
                "responsive": true,
            });
        });
    </script>
}
