@model BCKFreightTMS.Web.ViewModels.Shared.ApplicationModel

<div class="p-2">
    <a asp-action="DownloadApplication" asp-route-id="@Model.OrderId" id="downloadBtn" class="btn btn-primary">Download</a>
    <a href="@(Model.ReturnUrl??"/Orders/Index")" class="btn btn-secondary">Close</a>
</div>

<div class="mt-3 p-5 bg-white shadow" style=" width: 24cm; margin: auto;">
    @Html.Raw(Model.ApplicationHtml)
</div>

@section Scripts{
    <script>
        document.getElementById("downloadBtn").addEventListener("click", function () {
            setTimeout(function () {
                $(".loading").hide()
            }, 4000);
        })
    </script>
}